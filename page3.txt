<head>
  <title>Confirmation</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <link rel="shortcut icon" href="/static/logo.png" />

  <style>
    :root {
      --card-bg: rgba(255, 255, 255, 0.94);
      --text: #0f2317;
      --muted: #516257;
      --accent: #0f9b4c;
      --shadow: 0 18px 50px rgba(0, 0, 0, 0.22);
      --radius: 18px;
    }

    * { box-sizing: border-box; }

    html, body {
      height: 100%;
      margin: 0;
      font-family: "Helvetica Neue", Arial, sans-serif;
      background: #031307;
      color: var(--text);
    }

    body.bg {
      background-image: linear-gradient(120deg, rgba(3, 19, 7, 0.9), rgba(3, 19, 7, 0.65)), url("/static/background.png");
      background-position: center;
      background-size: cover;
      background-repeat: no-repeat;
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 28px;
    }

    .card {
      width: min(600px, 100%);
      background: var(--card-bg);
      border-radius: var(--radius);
      padding: 28px 26px 30px;
      box-shadow: var(--shadow);
      text-align: center;
      position: relative;
      overflow: hidden;
    }

    .card::after {
      content: "";
      position: absolute;
      inset: 0;
      border-radius: var(--radius);
      background: linear-gradient(140deg, rgba(15, 155, 76, 0.12), rgba(15, 155, 76, 0));
      pointer-events: none;
    }

    .logoimagestyle {
      display: block;
      margin: 0 auto 10px;
      width: min(240px, 55vw);
    }

    h1 {
      margin: 14px 0 10px;
      font-size: clamp(22px, 4vw, 26px);
      color: var(--text);
    }

    p {
      margin: 0;
      font-size: 15px;
      color: var(--muted);
      line-height: 1.5;
    }

    .cta {
      margin-top: 18px;
      display: inline-flex;
      align-items: center;
      justify-content: center;
      gap: 8px;
      padding: 12px 16px;
      border-radius: 12px;
      background: linear-gradient(120deg, var(--accent), #0d7c3e);
      color: #fff;
      font-weight: 700;
      text-decoration: none;
      box-shadow: 0 14px 30px rgba(15, 155, 76, 0.3);
    }
  </style>
</head>

<body class="bg">
  <section class="card">
    <img src="{{url_for('static', filename='logo.png')}}" class="logoimagestyle" alt="Restore and More" />
    <h1>Registration successful</h1>
    <p>Thanks for sharing your details. Our team will review your application and reach out shortly with next steps.</p>
    <a class="cta" href="/browse">Back to browse</a>
  </section>
</body>